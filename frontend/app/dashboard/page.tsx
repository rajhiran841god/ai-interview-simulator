"use client";

// Placeholder — populated in a later milestone once the Interview
// Intelligence Engine architecture (Milestone 2) has passed the
// Architecture Review Gate. For now this just confirms auth works.

export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gray-50 px-4">
      <div className="text-center">
        <h1 className="text-xl font-semibold text-gray-900">
          You're signed in.
        </h1>
        <p className="mt-2 text-sm text-gray-500">
          Dashboard — resume upload, JD upload, and interview start will
          land here in the next milestone.
        </p>
      </div>
    </main>
  );
}
